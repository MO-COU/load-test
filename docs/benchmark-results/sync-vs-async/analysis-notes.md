# 동기 적재 실험 — 분석 노트

Stage 0~4 측정 결과(각 `sync/stage-N.md`)를 보면서 나온 질문과 결론을
기록한다. 수치 표는 각 stage 문서에 있고, 여긴 **왜 그런 결과가 나왔는지**
설명이 목적이다.

## 1. Tomcat 스레드·HikariCP 풀은 몇 개가 적당한가

**원칙**: 스레드 수는 다운스트림 자원(DB 커넥션 풀)이 감당할 수 있는
만큼만 의미가 있다. 블로킹 I/O 워크로드(요청 처리 시간의 대부분이 DB
응답 대기)에서 스레드가 커넥션 풀보다 훨씬 많으면, 초과분은 그냥
**커넥션 자리를 기다리며 노는 스레드**가 된다.

우리 설정(스레드 200 / 풀 50 = 4배)이 정확히 이 상황을 보여줬다.
VU=200에서 스레드 200개가 다 찼는데, 그중 144~149개는 `hikari_pending`
— 실제로 일 안 하고 커넥션 빈 자리만 기다리는 상태였다. 이 150개는
처리량에 0 기여하면서 메모리(스레드당 스택)와 컨텍스트 스위칭 비용만
썼다.

### 트레이드오프

| 스레드를 | 얻는 것 | 잃는 것 |
|---|---|---|
| 늘리면 | 순간적으로 몰리는 요청을 더 많이 "접수"는 해줌(큐잉 여유) | 메모리·컨텍스트 스위칭 낭비. 풀이 못 받쳐주면 대기 줄만 길어져 p95만 나빠짐 |
| 줄이면 | 노는 스레드가 줄어듦, 초과 요청은 빨리 거절(fail-fast)돼서 시스템이 덜 흔들림 | 순간 버스트 흡수 여유가 줄어 짧은 스파이크에도 거절이 빨리 남 |

### 실무 기준

블로킹 I/O 워크로드에서 흔한 경험칙: **스레드 수 ≈ 커넥션 풀 크기의
1.5~2배**. 우리처럼 4배로 벌리진 않는다. 지금 설정 기준이면 풀=50 그대로
두고 스레드는 75~100 정도가 적당해 보인다 — 처리량 한계(어차피 DB 풀
50이 병목)는 그대로 유지하면서, 쓸데없이 노는 스레드를 줄여 메모리
여유를 확보하고 fail-fast를 더 낮은 VU에서 빨리 드러나게 한다.

## 2. 왜 풀·스레드를 무한정 늘려도 성능이 안 오르는가

결국 병목은 **MySQL 서버 자체가 실제로 동시에 처리할 수 있는 양**이다.
풀을 키우는 건 "문을 더 여는 것"이지 "일꾼을 늘리는 것"이 아니다.
MySQL 쪽에서 막히는 지점 3가지:

1. **CPU 코어 수** — 코어 개수만큼만 진짜 병렬로 일한다. 커넥션을 더
   열어도 코어 이상으로 동시에 실행되진 않고 컨텍스트 스위칭만 늘어난다.
2. **디스크 fsync 상한** — `innodb_flush_log_at_trx_commit=1`이라 커밋마다
   fsync한다. 초당 가능한 fsync 횟수는 디스크 하드웨어가 정한 물리적
   상한이라 커넥션을 아무리 열어도 못 넘는다.
3. **InnoDB 내부 락/뮤텍스 경합** — 버퍼 풀, redo log, 인덱스 페이지 같은
   공유 자료구조를 동시에 만지는 커넥션이 많을수록 서로 기다리는 시간이
   늘어난다(`Innodb_row_lock_waits` 증가로 관측).

**일정 지점을 넘으면 처리량이 오히려 떨어질 수도 있다.** 동시에 일하는
주체가 너무 많아지면 서로 부딪히고 기다리는 시간이 일하는 시간보다
커지는 현상(줄서기 이론의 "thrashing")이다.

## 3. 적정 크기를 실측하는 방법 (아직 안 함 — 방법만 정리)

지금까지 한 "VU 스윕"과는 축이 다르다. 그건 **부하(VU)를 고정된
풀·스레드에 던져서 어디서 터지는지** 봤고, 풀·스레드의 적정값을 찾으려면
**풀·스레드 크기 자체를 바꿔가며 처리량이 어디서 꺾이는지** 봐야 한다.

1. **커넥션 풀 스윕**: VU를 충분히 높게 고정(예: 200), `DB_POOL_SIZE`를
   10→20→30→50→75→100→150로 바꿔가며 앱을 재시작하고 매번 처리량을
   기록. 이론적 하한선은 Little's Law로 먼저 가늠: `필요 풀 크기 ≈
   목표 처리량(req/s) × 평균 커넥션 점유시간(s)`.
2. **Tomcat 스레드 스윕**: 1번에서 찾은 최적 풀 크기로 고정하고,
   `TOMCAT_MAX_THREADS`를 풀 크기의 1x/1.5x/2x/4x(현재값)로 바꿔가며
   비교. 처리량은 어느 시점부터 거의 안 바뀌어야 정상(병목이 이미 풀로
   넘어갔으므로) — 그 지점을 지나서 스레드를 올리는 건 메모리만 쓰는 것.

env var(`DB_POOL_SIZE`, `TOMCAT_MAX_THREADS`, `TOMCAT_ACCEPT_COUNT`)는
`application.yaml`에 이미 다 빠져 있어서 코드 수정 없이 재시작만으로
가능하다. 다만 이 스윕은 설정 바꿀 때마다 **앱을 재시작**해야 해서
VU 스윕보다 손이 훨씬 많이 간다 — 필요할 때 자동화 스크립트로 만들기로
하고 보류.

## 4. 로컬 측정 환경의 특성

- **호스트 CPU**: 12코어. Docker Desktop에도 12 CPU 전부 할당돼 있고
  `mysql-large` 컨테이너 자체엔 별도 CPU 제한 없음(`NanoCpus: 0`).
  즉 MySQL이 쓸 수 있는 CPU 상한 = 이 PC의 12코어 그대로지만, 앱·k6·
  Docker Desktop 자체와 나눠 쓰는 것이라 순간적으로는 이보다 적게 쓸 수
  있다.
- **`innodb_os_log_fsyncs`는 PC마다 다르다**: 물리 디스크 종류(NVMe/
  SATA/HDD)뿐 아니라, **Docker Desktop for Windows가 WSL2 위에 가상
  디스크(VHDX)를 만들고 그 안의 Docker volume에 MySQL이 쓰는 구조**라
  한 겹 더 가상화가 끼어 있다. 같은 PC라도 Docker Desktop 설정에 따라
  달라질 수 있는, **이 실험 환경에 고유한 값**이라 절대 수치를 다른
  환경과 비교하면 안 된다.

## 5. fsync 물리적 상한에 실제로 닿은 구간은 어디인가

fsync **횟수**가 아니라 **초당 처리율**(`fsync_delta ÷ 경과시간`)로
봐야 한다. 상한에 닿았다면 VU를 올려도 이 값이 평평해진다.

| Stage | VU 10 | VU 50 | VU 100 | VU 200 | VU 300 | VU 400 | VU 500 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Stage 1 (30만) | 891/s | 343/s | 341/s | 409/s | 347/s | 416/s | 396/s |
| **Stage 4 (300만)** | 178/s | 165/s | 155/s | 166/s | 169/s | 162/s | 162/s |

**Stage 4에서 가장 뚜렷한 상한 신호가 보인다.** VU를 10에서 500까지
50배 올렸는데도 fsync 처리율이 155~178/s 사이에서 거의 고정 — 이
환경의 물리적 fsync 상한에 도달한 것으로 해석한다. Stage 1~3은 343~900
/s 사이로 더 높고 들쭉날쭉한데, 이건 상한이라기보다 InnoDB의 **group
commit**(여러 트랜잭션 커밋을 fsync 한 번에 묶는 최적화)이 아직 여유
있게 작동한 결과로 본다.

## 6. Stage 4가 터진 원인 — 인덱스 부재? CPU? 버퍼 풀?

### 인덱스는 있다

`coupon_issue`엔 인덱스 3개가 다 있다(`PRIMARY`,
`uk_coupon_issue_coupon_member(coupon_id, member_id)`,
`idx_coupon_issue_member_issued_at(member_id, issued_at)`). **인덱스가
없어서가 아니라, 있는 인덱스의 크기가 캐시보다 커져서** 생긴 문제다.

### 버퍼 풀 = MySQL(InnoDB) 프로세스 자신의 메모리

"서버(OS)의 범용 버퍼"가 아니라 **MySQL이 기동할 때 확보하는 전용 RAM
캐시**다(`innodb_buffer_pool_size`). OS 파일 캐시도 별도로 있지만,
지금 병목은 그보다 상위 레이어인 InnoDB 자체 캐시다.

### 실측 근거

| 항목 | 크기 |
|---|---:|
| `coupon_issue` 데이터 | 177.7 MB |
| 인덱스 3개 합 | 193.0 MB |
| **합계** | **370.7 MB** |
| `innodb_buffer_pool_size` | **128 MB**(기본값, 미조정) |

테이블+인덱스가 캐시의 거의 3배라 65%가 디스크에 있다. Stage 0~3(≤150만
건)까진 테이블이 128MB보다 작아 다 캐시에 들어갔는데, Stage 4(300만
건)에서 처음 이 경계를 넘었다 — 시점이 정확히 일치한다.

### 그런데 CPU를 완전히 배제할 순 없다

- **수학적으로 CPU만으론 설명이 약함**: B+Tree 탐색 비용은 `O(log n)`.
  1만→300만 건이면 트리 깊이 차이는 8단계 정도(log₂(3,000,000) -
  log₂(10,000) ≈ 8)뿐이라, 이걸로 2.5배 지연 증가를 설명하긴 어렵다 —
  디스크 I/O 대기가 끼어들어야 설명이 된다.
- **하지만 물리 읽기(`innodb_buf_reads`) 수치는 깨끗하지 않다**: VU=10
  기준 Stage 1=0, Stage 2=822, Stage 3=**1,031**, Stage 4=927 —
  Stage 4가 Stage 3보다 오히려 적다. 버퍼 풀이 원인이면 Stage 4에서
  확 튀어야 하는데 그렇지 않다.
- **놓친 변수**: Stage 4 시딩(300만 건, 52초)을 하고 바로 VU 스윕을
  돌려서, dirty page flush가 덜 끝난 상태가 섞였을 가능성이 있다.
  CPU 사용률도 한 번도 직접 측정한 적이 없다(`docker stats` 안 씀).

**결론**: 버퍼 풀(디스크 I/O) 쪽이 CPU보다 유력하지만 확정은 아니다.
검증하려면 (1) `innodb_buffer_pool_size`를 512MB~1GB로 올려 Stage 4
재측정(버퍼 풀 원인이면 VU=10 기준선이 8ms대로 복귀해야 함), (2)
테스트 중 `docker stats`로 CPU 실측, (3) 시딩 후 정착 시간을 두고
재측정 — 세 가지가 필요. **사용자 요청으로 보류 중.**

## 7. 비동기(배치) 적재로 바꾸면 예상되는 변화

### 이 비교에서 Tomcat·커넥션 풀은 최종 판단 기준이 아니다

비동기가 Tomcat 스레드·HikariCP 풀을 훨씬 여유 있게 쓴다는 건 사실이다
(아래 참고). 하지만 이건 **"HTTP 요청이 DB까지 직접 간다"는 지금
벤치마크 API(`BenchmarkCouponIssueController`) 고유의 특성 때문이지,
실제 목표 아키텍처의 특성이 아니다.** 실제 목표는 Redis(Lua)가 이미
재고 판정을 끝낸 뒤의 DB 적재 단계만 비교하는 것이고, 그 아키텍처에서는
HTTP 스레드가 애초에 DB를 직접 안 건드린다. **그래서 Tomcat/풀 포화는
이 결과를 읽을 때 참고할 맥락일 뿐, 동기·비동기 중 뭘 최종 채택할지
정하는 기준으로는 쓰지 않는다.**

또한 "스레드가 노는 낭비"는 동기 방식 자체의 근본적 한계가 아니라
**Tomcat/풀 설정(200/50, 4배)을 잘못 튜닝한 결과**다(1장 참고). 스레드
수를 풀 크기에 맞게 줄이면 동기 방식도 이 낭비는 거의 없앨 수 있다.
그래서 "스레드가 안 논다"는 async의 근본적 장점 목록에서 뺀다 — 이건
튜닝으로 해결 가능한 문제였다.

### 튜닝으로는 못 없애는, 진짜 구조적인 async의 장점 두 가지

1. **배치는 같은 물리적 fsync 상한 안에서 실질 처리량을 올린다.** 동기는
   요청-응답이 1:1로 묶여 있어 구조적으로 여러 요청을 한 커밋에 못
   묶는다. 비동기는 N건을 한 트랜잭션에 묶어 fsync 1번으로 N건을 처리할
   수 있다. Stage 4에서 본 "fsync 처리율이 VU 무관하게 145~180/s로
   고정"되는 상한(5장) 자체는 안 바뀌지만, 그 상한 안에서 실어 나르는
   행 수는 배치 크기만큼 늘어난다. **단, 이건 컨슈머 코드에 배치 로직을
   직접 구현해야 얻어지는 효과이지, 큐나 Kafka를 쓴다고 자동으로 따라오는
   게 아니다.** 컨슈머가 메시지를 1건씩 읽어 1건씩 INSERT하면 동기와
   똑같이 fsync 이득이 0이다.
2. **한계를 넘었을 때 실패 방식이 "거부"에서 "지연"으로 바뀐다.** 동기는
   한계를 넘으면 Tomcat/OS가 TCP 연결 자체를 거부하고 요청이 그대로
   유실된다(Stage 0 VU=500, 3장). 비동기는 한계를 넘으면 큐/Stream 적체가
   늘고 DB 반영까지 걸리는 시간이 늘어날 뿐, 사용자 응답 자체는 계속
   빠르게 나간다. 물리적 한계를 없애는 게 아니라 한계에 부딪혔을 때의
   증상을 바꾸는 것이고, **적체를 안전하게 버틸 곳이 있어야 유효하다**
   (Redis는 메모리 기반이라 한계가 낮고, Kafka 같은 디스크 기반 큐는
   훨씬 오래 버틴다 — 9장 참고).

### 참고: Tomcat·커넥션 풀 개선 (최종 결정엔 미반영)

- VU=200 근처 스레드 포화가 뒤로 밀릴 것으로 예상 — 요청 스레드가 DB
  응답을 기다리며 블로킹되지 않고 큐에 넣고 바로 응답하므로.
- VU=500의 TCP 연결 거부 실패가 줄거나 훨씬 높은 VU에서만 나타날 것으로
  예상 — 스레드가 안 막히니 Tomcat accept 큐가 순간적으로 넘칠 확률이
  낮아짐.

### 여전히 안 풀릴 것

1. **Stage 4의 동시성과 무관한 기본 지연 증가** — 버퍼 풀 초과가 원인이면
   동기든 비동기든 같은 인덱스 페이지를 읽고 쓰는 건 똑같아서 안 없어짐.
2. **MySQL 서버 자체의 물리적 한계**(CPU 12코어, 디스크 I/O 대역폭) —
   비동기는 "얼마나 오래 기다리게 하느냐"를 바꾸는 거지 "일꾼 수"를
   늘리는 게 아니다.
3. **Row lock 경합**은 배치로 묶어도 근본적으로 남고, 배치 크기를
   잘못 잡으면 한 트랜잭션이 여러 행을 더 오래 잠가 오히려 나빠질 수도
   있다.

### 새로 생기는 문제

1. **정합성 유실 창** — "응답 옴 = 큐에 들어감"으로 의미가 바뀐다.
   응답 직후 DB를 조회하면 아직 없을 수 있는 시간차가 생긴다.
2. **크래시 시 데이터 유실** — 가장 단순한 구현(인메모리 큐)이면 큐에
   쌓인 채로 앱이 죽으면 그 요청들은 통째로 사라진다. 동기보다 오히려
   나빠질 수 있는 지점 — Redis Stream 같은 영속 큐를 쓰기로 한 이유.
3. **백프레셔** — 들어오는 속도가 비우는 속도보다 계속 빠르면 결국
   큐가 무한정 쌓인다. 지금은 "연결 거부"로 명확히 터졌는데, 비동기는
   대신 메모리 부족으로 죽거나 응답 지연이 한없이 느는 형태로 나타날
   수 있다 — 새로 측정해야 할 실패 모드.

### 실측 결과로 확인 (Stage 0, 2026-08-19)

위 예측을 실제로 구현(`AsyncCouponIssueConsumer` — 단일 스레드,
`BATCH_SIZE`/`BATCH_INTERVAL_MS` 둘 중 먼저 차는 조건으로 커밋)하고
Stage 0에서 측정했다. 상세 표·해석은
[`async/stage-0.md`](async/stage-0.md), 요약은 다음과 같다.

- **예측대로 확인된 것**: VU=200 근처 Tomcat/풀 포화가 사라졌고
  (요청 스레드가 DB를 안 기다리므로 당연), INSERT+UPDATE VU=500 실패율이
  24.11%(동기) → 0.25~0.49%(비동기)로 급감했다.
- **예측과 달리 응답 지연에는 배치 유무가 안 드러난다** — nobatch와
  batch100의 Avg/p95는 거의 겹친다. 이유는 명확하다: 응답은 "큐에
  넣었다"만 확인하고 끝나서, 컨슈머가 몇 건씩 묶어 커밋하는지는 응답
  경로에 아예 반영되지 않는다. 배치 효과는 **drain 시간**(k6 종료 후
  큐가 실제로 0이 될 때까지 걸리는 시간)에서만 보인다 — batch100은
  VU와 무관하게 6~12초로 평평, nobatch는 VU가 오를수록 늘어나다
  60초 컷오프에 막혀 못 비운다.
- **예측에 없던 새 발견**: nobatch(배치 없이 커밋마다 fsync 1번)는
  단일 컨슈머 스레드의 처리 상한이 초당 ~170~200건 정도로 낮아서,
  INSERT+UPDATE 워크로드(요청 1건 = 이벤트 2개)는 **VU=10에서조차
  60초 안에 다 못 비웠다.** 이건 "배치를 안 쓰면 이득이 없다"보다
  훨씬 나쁜 결과다 — 배치 없는 비동기는 동기보다도 못한, **컨슈머
  스레드 하나로 모든 부하를 직렬 처리하는 새로운 병목**을 만든다.
  동기는 최대 200개 스레드가 동시에 커밋하지만, 배치 없는 비동기는
  1개 스레드가 순서대로 커밋한다 — "비동기로 바꿨다"는 사실 자체가
  아니라 "직렬화된 소비자"가 원인이다.
- **(2026-08-20 추가 확인)** 정합성(적재 성공 건수)은 별도 소규모
  재현 테스트로 확인했다 — nobatch/batch100 × INSERT만/INSERT+UPDATE
  4개 조합 모두, 큐가 완전히 빌 때까지 기다리면 요청 건수와 DB 반영
  건수가 정확히 일치했다(유실·중복 0건). nobatch의 60초 컷오프는
  "데이터가 없어진다"가 아니라 "느려서 관측 시간 안에 못 끝난다"는
  뜻이었다 — 다만 VU 단계 간 백로그가 섞여 fsync 등 세부 지표를
  오염시켰을 가능성은 여전히 남아있다. 상세는
  `async/stage-0.md`의 "아직 못 채운 것" 참고.
- **(2026-08-20 Stage 1~4 측정 후 추가)** Stage 0의 결론이 데이터
  규모가 커져도(1만→300만) 그대로 유지되는지 마저 확인했다.
  batch100의 drain 시간은 Stage 0~4 전 구간에서 6~7초/12~14초로
  완전히 평평했다 — 배치가 데이터 크기 자체에 거의 영향을 안 받는다는
  뜻이다. 반대로 nobatch는 Stage 0~1(30만 이하)까진 어떻게든 느리게나마
  다 비웠는데, Stage 2(90만)부터는 VU=10에서조차 60초 안에 못 비운다 —
  데이터가 커질수록 "배치 없는 비동기"가 동기보다 더 빨리 무너질 수
  있다는 뜻이다. 한 가지는 예상 밖이었다: 6장에서 다룬 동기 Stage 4의
  버퍼 풀 병목(VU=10 기준선이 8ms→21ms로 2.5배 느려짐)이 비동기
  batch100의 drain 시간에는 재현되지 않았다(Stage 4도 Stage 0과 똑같이
  6~7초). coupon_id=4 인덱스 범위가 반복 실행으로 계속 캐시에 남아있어서
  그런 건지, 아니면 sync의 저하가 사실 버퍼 풀보다 동시 커넥션 200개의
  잠금/래치 경합 쪽에 더 가까운 건지 확정하지 못했다 — 컨슈머가
  커넥션 1개(스레드 1개)뿐이라 후자라면 애초에 그 경합이 없다.
  상세는 `async/stage-1-to-4.md` 참고.

## 8. 인메모리 큐 vs Redis — 메모리 기반이면 둘 다 똑같이 죽는 거 아닌가

아니다, 결정적 차이가 있다.

| | 앱의 인메모리 큐(순진한 구현) | Redis |
|---|---|---|
| 메모리 위치 | Spring Boot 앱의 JVM 힙 — Tomcat·비즈니스 로직과 같은 메모리 | 별도 프로세스, 독립된 메모리 공간 |
| 가득 찼을 때 | 정해진 게 없음 → 계속 커지다 `OutOfMemoryError` | `maxmemory` 설정 + eviction 정책으로 동작을 직접 정할 수 있음 |
| 죽으면 | **앱 전체가 죽음**(Tomcat도 다른 API도 같이) | Redis만 영향받음, 앱은 재시도/서킷브레이커로 대응 가능 |

Redis를 쓴다고 저절로 안전해지는 게 아니라, **가득 찼을 때 뭘 할지
명시적으로 정해야 한다.** `maxmemory-policy noeviction`(꽉 차면 새
쓰기 거부) + Stream의 `MAXLEN`(최대 길이 제한)으로 "이 이상 쌓이면
새 요청을 거부한다"는 정책을 직접 정해야, 지금 동기 방식에서 본
"TCP 연결 거부"에 대응하는 **깨끗한 실패 모드**를 비동기에서도 가져갈
수 있다. `allkeys-lru` 같은 정책은 큐 용도로는 위험하다 — 아직 DB에
안 들어간 대기 중인 데이터가 지워질 수 있어서, 정합성을 지키려고
Redis Stream을 쓰기로 한 목적과 정면으로 배치된다.

## 9. Kafka vs Redis Stream — 왜 비교 대상인가

### 같은 역할을 다투는 두 후보다, 순서대로 이어지는 단계가 아니다

파이프라인엔 "빈 자리"가 하나뿐이다: `Lua(재고 차감+outbox 기록) → ???(버퍼) → ???(컨슈머, DB 배치 쓰기)`.
Redis Stream을 쓰면 저 "???" 둘 다 Redis가 맡는다(`XADD`로 버퍼링,
Stream 자체의 컨슈머 그룹 기능(`XREADGROUP`/`XACK`)으로 우리 컨슈머가
직접 읽어서 DB에 씀). Kafka를 쓰면 저 자리 둘 다 Kafka로 바뀐다. **"Redis
Stream(outbox) 뒤에 Kafka가 추가로 붙는" 게 아니라, 같은 한 자리를
무엇으로 구현할지의 선택**이다.

### 그런데 Redis Stream은 완전히 대체 가능한가 — 아니다, Lua 제약 때문에

Lua 스크립트는 Redis 내부에서만 실행되고 외부 네트워크 호출(Kafka
포함)을 할 수 없다. "재고 차감 + 발급자 등록 + 이벤트 기록"을 원자적으로
묶으려면, 그 이벤트 기록은 **반드시 Redis 안의 무언가**여야 한다. 그래서
Redis Stream은 이 원자적 단계에서 Kafka로 대체할 수 없고, Kafka는 그
뒤(릴레이를 거쳐)에서만 붙을 수 있다.

이건 "Lua를 써서" 생기는 제약이 아니라 "판단과 이벤트 기록을 원자적으로
묶고 싶어서" 생기는 제약이다. WATCH/MULTI/EXEC 방식으로 원자성을
얻어도 마찬가지다 — `MULTI` 블록 안에 `DECR`, `SADD`와 함께 `XADD`를
같이 큐잉하면 Lua와 동일한 원자성을 얻는다. EXEC 이후에 별도로 Kafka를
직접 호출하는 방식도 기술적으론 가능하지만, 그 순간 "이중 쓰기 문제"
(EXEC와 Kafka 호출 사이에 앱이 죽으면 이벤트가 유실)가 재발한다.

### Kafka를 뒤에 추가로 얹을 때(outbox 패턴의 정석: Redis Stream → 릴레이 → Kafka)

이 구조에서 Redis Stream의 역할은 "outbox"로 좁아진다 — Lua가 기록한 걸
아주 가벼운 릴레이가 거의 즉시 집어 Kafka로 넘기고, Redis Stream
엔트리는 바로 지운다. Redis가 오래 들고 있을 이유가 없어지고, 적체·
재처리·장기 보존은 전부 Kafka 몫이 된다.

**장점**
- Kafka는 디스크 기반이라 컨슈머가 못 따라가도 Redis(메모리 기반)보다
  훨씬 안전하게 적체를 버틴다.
- Kafka는 장기 보존이 가능해(며칠~몇 주), 사고 후 DB 반영 여부를
  대조·재처리하는 감사 로그로 쓸 수 있다. Redis Stream은 메모리 보호를
  위해 `MAXLEN`으로 계속 잘라야 해서 이 용도로는 약하다.

**단점**
- 컴포넌트가 2개 늘어난다(릴레이 프로세스, Kafka 클러스터).
- 릴레이 자체가 새 장애 지점이다 — 릴레이가 느려지거나 죽으면 다시
  Redis Stream에 적체가 쌓인다(위험이 없어진 게 아니라 옮겨감. 다만
  릴레이는 SQL·락·배치 커밋이 없어 DB 쓰기보다 훨씬 안 느려질
  가능성이 높다).
- 홉이 늘어난 만큼(Redis→릴레이→Kafka→컨슈머) 종단 지연이 늘어난다.
- Kafka 클러스터(브로커, ZooKeeper/KRaft)를 계속 운영해야 하는 지속적
  비용이 생긴다.

### 메모리 부하가 두 배가 되는가 — 아니다, 자원 종류가 다르다

Redis Stream(RAM)과 Kafka(디스크+페이지 캐시)는 **같은 자원을 두 번
쓰는 게 아니라 서로 다른 자원을 쓴다.** Redis 쪽 사본은 릴레이가 빨리
집어가면 짧게 스쳐가고, Kafka는 RAM을 크게 잡아먹는 구조가 아니다.
다만 **릴레이가 못 따라가면 그 순간엔 Redis에도 데이터가 쌓이기 시작해
두 시스템 모두에 부담이 겹칠 수 있다** — 릴레이 자체의 처리량도
모니터링 대상에 넣어야 하는 이유다.

### 프로듀서 쪽에서 Kafka 응답을 기다리면 그것도 병목 아닌가

맞는 우려이지만 피할 방법이 있다. Kafka 프로듀서 클라이언트는 원래
**클라이언트 쪽에서 여러 메시지를 모아뒀다가 배치로 비동기 전송**하도록
설계돼 있다(`linger.ms`, `batch.size`, 콜백 기반 ack). 한 건마다 동기로
기다리게 짜면 MySQL 커밋 대기와 같은 문제가 재발하고, 배치+비동기로
짜면 피할 수 있다 — 이것도 "자동으로 되는 게 아니라 그렇게 구현해야
하는" 부분이다(7장의 배치 논의와 같은 원리).

## 10. "결국 같은 물리적 한계 아닌가" — 낭비 vs 진짜 구조적 차이

CPU 코어 수, 디스크 fsync 상한 같은 물리적 한계(2장)는 아키텍처를
어떻게 짜든 절대 못 넘는다. Kafka나 비동기를 쓴다고 이 한계 자체가
올라가지 않는다. 다만 "같은 한계인데 뭐가 달라지는가"는 세 가지로
나뉜다.

1. **낭비 제거는 async의 장점이 아니다.** 동기에서 스레드가 노는 건
   Tomcat/풀 설정을 잘못 튜닝한 결과다(7장에서 이 논거를 뺀 이유).
   튜닝만으로 해결되는 문제를 async의 장점으로 세면 안 된다.
2. **배치는 진짜로 실질 처리량을 올린다** — 같은 fsync 상한 안에서
   커밋 하나에 실어 나르는 행 수를 늘리는 것이라, 튜닝으로는 흉내 낼 수
   없다(동기는 요청-응답이 1:1이라 여러 요청을 한 커밋에 못 묶음).
3. **한계를 넘었을 때의 실패 모드가 바뀐다** — "거부"에서 "지연"으로.
   이것도 튜닝으로는 못 바꾼다(동기는 HTTP 응답을 그 요청 안에서 끝내야
   하니 무한정 기다리게 할 수 없음).

**로컬 실험 환경 한정 주의**: 지금처럼 Kafka·Redis·MySQL·앱을 전부 같은
PC 한 대(12코어)에 띄우면, Kafka도 결국 그 한정된 CPU/RAM을 나눠 쓰는
새 참가자일 뿐이다. 즉 로컬 벤치마크에서는 "전체 시스템의 자원 총량이
늘어나는 것"이 아니라 위 1~3번의 효율·실패 모드 개선만 유효하다. 실제
운영 환경(Kafka가 별도 서버)이라면 이 caveat는 없다.

## 부록: 측정 중 있었던 사고

`scripts/large-db/run-vu-sweep.sh`의 이어쓰기(append) 로직에 버그가
있었다 — `grep -E`의 `'\t'`가 이 환경에서 실제 탭 문자로 해석되지
않아, VU=300을 추가할 때 Stage 0의 기존 요약(10~500, 333)이 전부
사라지고 300만 남았다. k6 로그 원본이 남아있어 전부 복구했고
(`sync/stage-0.md`의 표가 최종본), 스크립트는 `$'\t'`로 실제 탭을
만들어 쓰도록 고쳤다. 원본 raw 데이터는 유실되지 않았다.