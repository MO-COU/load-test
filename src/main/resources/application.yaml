spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:coupon}?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME:coupon}
    password: ${DB_PASSWORD:coupon1234}
    hikari:
      # 통일 사항. 브랜치별로 바꾸면 비교가 성립하지 않는다.
      maximum-pool-size: ${DB_POOL_SIZE:50}
      connection-timeout: ${DB_CONNECTION_TIMEOUT:10000}

  jpa:
    hibernate:
      # 스키마는 scripts/db/schema.sql 이 관리한다.
      ddl-auto: ${JPA_DDL_AUTO:validate}
    show-sql: ${JPA_SHOW_SQL:false}
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: ${REDIS_TIMEOUT:3000ms}
      lettuce:
        pool:
          max-active: ${REDIS_POOL_MAX_ACTIVE:3000}
          max-idle: ${REDIS_POOL_MAX_IDLE:3000}
          min-idle: ${REDIS_POOL_MIN_IDLE:50}

server:
  port: ${SERVER_PORT:8080}
  tomcat:
    threads:
      # 통일 사항.
      max: ${TOMCAT_MAX_THREADS:200}
    accept-count: ${TOMCAT_ACCEPT_COUNT:100}
    # 통일 사항. 기본값 8192 로는 VU 20000 의 연결을 다 받지 못한다.
    max-connections: ${TOMCAT_MAX_CONNECTIONS:30000}
    # 스레드풀 사용률 확인을 위한 mbean 등록
    mbean-registry:
      enabled: true
# ~/metrics.sh 가 /actuator/prometheus 를 긁는다.
management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_EXPOSURE:prometheus,metrics,health}
